# Copyright Vespa.ai. Licensed under the terms of the Apache 2.0 license. See LICENSE in the project root.

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-query-container
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.query.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-query-container
      name: {{ .Release.Name }}-internal
  serviceName: {{ .Release.Name }}-internal
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-query-container
        name: {{ .Release.Name }}-internal
    spec:
      containers:
      - name: vespa-query-container
        image: {{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}
        args: ["services"]
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: {{ .Release.Name }}-config
        securityContext:
          runAsUser: 1000
        resources:
          requests:
            memory: "1.5G"
          limits:
            memory: "1.5G"
